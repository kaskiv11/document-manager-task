# Document Manager Task

Application for managing documents with features for saving, searching, and retrieving documents by ID. Data is stored in an in-memory collection.

## Installation

To build the project, run:

```bash
mvn clean install
